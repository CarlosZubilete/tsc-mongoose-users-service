# ============================================================================

# DOCKER BUILD CONTEXT EXCLUSIONS

# These patterns prevent unnecessary files from being copied into the Docker

# build context, reducing build context size and improving build performance.

# ============================================================================

# Documentation and development guides (not needed in production)

doc/
README.md
\*.md

# Node.js and package management

node_modules/
dist/
pnpm-store/
.pnpm-debug.log

# Environment and secrets (must be injected at runtime, never baked into image)

.env
.env.local
.env.\*.local

# Version control and metadata

.git/
.gitignore
.gitattributes

# IDE and editor configurations (development-only files)

.vscode/
.idea/
_.swp
_.swo
\*~
.DS_Store
.env.example

# Testing and temporary files

.mocharc.json
.eslintrc*
.prettierrc*
jest.config.js
coverage/
\*.log

# CI/CD and deployment configurations (not needed in runtime image)

.github/
.gitlab-ci.yml
.circleci/

# HTTP test files (API testing, not needed in production)

_.http
_.rest

# Database persistence and temporary data

mongo-data/
mongodb/

# Docker and build artifacts

Dockerfile
.dockerignore
docker-compose.yml

# npm/yarn lock files (we use pnpm-lock.yaml only)

package-lock.json
yarn.lock